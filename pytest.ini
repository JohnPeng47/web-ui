[tool:pytest]
# Add parent directory to Python path for imports
python_paths = .

# Optional: Specify test directories to limit conftest.py scope
testpaths = tests

# Optional: Additional pytest configuration
addopts = -s